[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "closeout"
version = "0.1.0"
description = "Shot-quality model built on 2015-16 NBA player-tracking data"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
